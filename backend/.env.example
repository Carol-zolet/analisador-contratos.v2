# Backend env example

# String de conexão para a base de dados
# SQLite (local, padrão)
DATABASE_URL=sqlite:///./contratos.db

# PostgreSQL (Azure Database for PostgreSQL)
# DATABASE_URL=postgresql://USUARIO:SENHA@SERVIDOR.postgres.database.azure.com:5432/postgres?sslmode=require

# PostgreSQL (Render, Railway, outras clouds)
# DATABASE_URL=postgresql://usuario:senha@host:5432/nome_do_banco

# PostgreSQL (Heroku/Railway - string completa)
# DATABASE_URL=postgres://usuario:senha@host:5432/banco

# Origens permitidas para CORS (separadas por vírgula)
ALLOWED_ORIGINS=http://localhost:5173

# Extensões de arquivo aceitas (separadas por vírgula)
ALLOWED_EXTS=pdf,docx

# Tamanho máximo de upload em MB
MAX_UPLOAD_MB=15

# Criar tabelas automaticamente ao iniciar (true/false)
AUTO_CREATE_TABLES=true

# Chave da API do Google Gemini para análise por IA
GEMINI_API_KEY=
